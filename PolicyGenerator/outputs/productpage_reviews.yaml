apiVersion: networking.k8s.io/v2
policyType: NetworkPolicy
metadata:
  name: productpage-microseg-policy
spec:
  podSelector:
    matchLabels:
      serviceName: productpage
  egress:
  - to:
    - podSelector:
        matchLabels:
          serviceName: reviews
  policyTypes:
  - Egress
